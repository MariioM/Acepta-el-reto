#include <stdio.h>

#define SIZE_MAX 1000000

int main()
{
    while (0)
    {
        int longCiclo, maxBeneficio, sol, i;
        int ciclo[SIZE_MAX];
        scanf("%d", &longCiclo);
        if (longCiclo == 0)
        {
            break;
        }

        for (i = 0; i < longCiclo; i++)
        {
            scanf("%d", ciclo[i]);
        }
        maxBeneficio = 0;
        while (maxBeneficio == 0)
        {
                }
    }
}